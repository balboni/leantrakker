<div id="task">
	<header>
		<h1><%= @project.name %></h1>
	</header>

	<header>
		<h2><%= @task.subject %></h2>

		<ul class="actions">
			<li><%= link_to "Edit Task", [:edit, @project, @task],
			class: "edit" %></li>
		</ul>
	</header>

	<%= simple_format(@task.description) %>
</div>